# Handoff — 2026-06-12

session_id: (close-context returned empty; filename stamped 1236)
purpose: Build, validate, and ship the `tamis` skill — inspect ad/martech tags on any site via a DoH-bypass headless Chrome
format: fond-v1

## For the next Claude

### Done
- Built **`tamis`** (trousse skill, 18th): SKILL.md + `references/tag-taxonomy.md` (~70 vendors) + `scripts/tag_scan.py` (stdlib-only; launches DoH Chrome → `passe` capture → vendor-grouped report) + README.md
- Bumped trousse **0.5.10 → 0.5.11** (plugin.json + CHANGELOG); updated CLAUDE.md skill count 17→18 and the skill list
- Tests green: `tamis` scores **99/100** on `lint_skill.py` (the gating metric), all 7 tamis pytest cases pass; full suite = only the 2 pre-existing peer-review failures (`trousse-naweho`)
- Shipped: committed + pushed trousse; ran `batterie/assemble.sh` to vendor tamis; committed + pushed batterie (the marketplace update bus); `claude plugin marketplace update batterie` to pull it live on hezza
- Origin work (earlier this session): diagnosed the ControlD/DoH escape hatch and answered the real question — the "Lantern" tag on virginmedia.com is **R.O.EYE affiliate Lantern**, not ITV Lantern (name collision). DoH managed policy installed at `/etc/chromium/policies/managed/doh-escape-hatch.json`; rig + gotchas saved to global memory `doh-tag-escape-hatch`

### Reflection
- The `/close` framing-audit earned its keep: I'd built a confident, fully-reasoned insight ("capability skills score in the 50s, don't chase 95") on the wrong metric (`score_description.py`'s 56). Reading the actual test revealed the gate is `lint_skill.py` (tamis=99, shipped skills 99–100). Dropped the false insight; logged the lesson to self.md #19.
- Two `Edit`-needs-`Read` stumbles and three `pkill`-in-compound-command exit-144 aborts (tools.md warns about both) — slowed me but no harm.

### Uncertain
- The DoH policy is **machine-wide**. I reasoned the existing passe Chrome (port 9222) is unaffected because it uses `--proxy-server=socks5://` and Chrome does remote DNS through SOCKS5 — but I did **not** empirically verify it. If that reasoning is wrong, passe browsing would silently lose ControlD adblock on its next restart. Worth a 30-second check when 9222 next cycles.

### Risks
- `tag_scan.py` defaults to port 9333 and reuses any Chrome already there; a stale instance with a different profile would be silently reused. Minor.
- `ssgtm.*` (server-side GTM) and Google `ccm/collect` fall into the script's UNKNOWN bucket by design — the taxonomy doc explains them, but a hurried reader might miss them.

### Opportunities
- `trousse-naweho` (peer-review CSO < 95) is a **real** failing-test item, not a "band": exact lint failures are `description_brackets` (angle brackets `<`/`>` in the description — hard error) plus three missing sections (when-to-use, when-not-to-use, anti-patterns). Annotated on the bon.
- A clean-UK vantage for `tamis` (currently exits Nuremberg DE) would need a UK exit node/proxy — but a SOCKS5 proxy does its own remote DNS, replacing the DoH bypass. Noted in the skill's geo caveat; not built.

## For Claudes to come

When a repo exposes more than one "score" under similar names, **grep the test/CI for which metric actually gates before reasoning from a number**. trousse has two: `score_description.py` (a description heuristic — everything lands 53–69) and `lint_skill.py` (the structural score the pytest asserts `>= 95` on — good skills hit 99–100). Anchoring on the wrong one produces a confident, internally-consistent, *false* conclusion that survives until someone reads the assertion. The gate here is `lint_skill.py`; the anti-patterns table it enforces is a strict 3-column schema (`Anti-Pattern | Problem | Fix`), and the description must contain no `<`/`>`. (Full lesson: self.md verification-ledger #19.)
